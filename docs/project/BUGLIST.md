# Debug List — Open Items

From CA review (2026-03-13). Most items resolved. Remaining:

| #   | Item                            | Notes                                                                               |
| --- | ------------------------------- | ----------------------------------------------------------------------------------- |
| 5   | **Camera framing from bounds**  | Use centroid for target, zoom based on bounds extents. Add to DRESS_BUILD_PROTOCOL. |
| 6   | **"Materials from geo 1" rule** | Every geo should get at least a color variant from the start. Add to OCTANE_MCP.    |

Items #1-2 (material type decisions), #3 (P_DIFFUSE retest), #4 (wait time), #7-9 (chrome, paths, placement pin) — all resolved.
